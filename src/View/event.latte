<!doctype html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <title>Document</title>
</head>
<body>
<form action="/index.php" method="POST">
    <p><a href="http://localhost:8000/index.php">Event SignUp</a> -> Event Details</p>
    <p>Event Name: {$event['name']}</p>
    <p>Date: {$event['date']}</p>
    <p>Description: {$event['description']}</p>
    <p>Joined/Max Participants: {$event['crntPeople']} / {$event['maxPeople']}</p>
    <p>Joined Users:<br>
            {foreach $participants[$event["id"]] as $username}
                {$username}<br>
            {/foreach}
        </p>
</form>

{foreach $participants[$event["id"]] as $user}
        {if $user === $_SESSION['username']}
            {var $joinedevent = true}
        {/if}
{/foreach}

{if $joinedevent === true}
        <a href="?leaveevent={$event["id"]}">Leave</a>
{else}
        <a href="?joinevent={$event["id"]}">Join!</a>
{/if}

</body>
</html>
